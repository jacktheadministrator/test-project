pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('Run test') {
            steps {
                echo 'Running test.sh'
                sh "chmod +x -R ${env.WORKSPACE}"
                sh './test.sh'
            }
        }
        stage('uname -a') {
            steps {
                echo 'Running uname -a'
                sh 'uname -a'
            }
        }
        stage('yum list installed') {
            steps {
                echo 'yum list installed'
                sh 'yum list installed'
            }
        }
    }
}
